# 6월 11일 작업 이슈 정리

> 오늘 실제로 연결되는 GitHub Issue 기준 정리.

---

## 오늘 메인 이슈

## Issue 5. 회원가입, 로그인, JWT 인증 구현

### 목표

사용자별 운동 기록을 구분할 수 있게 인증 기능을 만든다.

### 할 일

- [ ] AuthModule 생성
- [ ] AuthController 생성
- [ ] AuthService 생성
- [ ] SignupDto 생성
- [ ] LoginDto 생성
- [ ] 회원가입 API
- [ ] 로그인 API
- [ ] 비밀번호 hash 저장
- [ ] JWT accessToken 발급
- [ ] JWT Guard
- [ ] 내 정보 조회 API
- [ ] 게시글 작성 시 로그인 사용자 연결
- [ ] 작성자만 수정 가능하게 처리
- [ ] 작성자만 삭제 가능하게 처리

### 완료 기준

- `POST /auth/signup`이 동작한다.
- `POST /auth/login`이 accessToken을 반환한다.
- `GET /auth/me`가 Bearer token으로 내 정보를 반환한다.
- 로그인한 사용자만 게시글을 작성할 수 있다.
- 작성자만 자신의 게시글을 수정/삭제할 수 있다.

---

## 같이 확인할 이슈

## Issue 4. 운동 기록 게시글 CRUD API 구현

### 현재 상태

코드 기준으로 아래 API가 있다.

```text
POST /posts
GET /posts
GET /posts/:id
PATCH /posts/:id
DELETE /posts/:id
```

### 오늘 확인할 것

- [ ] `PATCH /posts/:id` curl 검증
- [ ] `DELETE /posts/:id` curl 검증
- [ ] 없는 게시글 수정 시 404 확인
- [ ] 없는 게시글 삭제 시 404 확인
- [ ] 운동 목록 포함 수정 요청 확인

### 완료 기준

Issue 4는 오늘 확인 후 닫아도 된다.

다만 인증이 붙으면 `PATCH`, `DELETE` 동작이 조금 바뀌므로, Issue 5 작업과 같이 검증하는 것이 좋다.

---

## 오늘 시작만 할 수 있는 이슈

## Issue 6. React 게시판 화면 연결

### 오늘 여유가 있을 때만

- [ ] API helper 생성
- [ ] 로그인 페이지 생성
- [ ] 회원가입 페이지 생성
- [ ] accessToken 저장

### 오늘 완료 목표는 아님

Issue 6은 금요일 메인 작업으로 넘긴다.

---

## 오늘 하지 않을 이슈

오늘은 아래 이슈를 건드리지 않는다.

```text
#7 댓글, 태그, 검색, 페이징
#8 FastAPI AI 서버 기본 세팅
#9 AI 운동 기록 분석 기능 구현
#10 RAG 기능 구현
#11 MCP tool 구현
#12 Agent workflow 구현
#13 README와 발표 준비
```

이유:

```text
인증이 없으면 사용자별 게시글, 내 기록, 작성자 권한, 이전 기록 비교가 전부 애매해진다.
```

